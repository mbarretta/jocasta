# Registry configuration. `init` fills in `team` with your team's name; the
# other two keys are set by the machinery and change only when you move this
# instance to a newer release of mbarretta/jocasta.
schema_version: 1
team: PLACEHOLDER_TEAM
machinery_ref: v1
